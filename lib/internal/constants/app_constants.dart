class AppConstants {
  static const POPPINS = 'Poppins';
  static const DEFAULT_PADDING = 20.0;
  static const DEFAULT_DURATION = Duration(seconds: 1); // we use it on our animation
  static const MAX_WIDTH = 1440.0;
}